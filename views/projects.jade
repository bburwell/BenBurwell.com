- nav = true;
extends layout
block content
    each project in projects
        .project.doc
            h1
                a(href=project.link)= project.title
            .description= project.description
